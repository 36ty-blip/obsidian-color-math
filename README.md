# 🎨 Obsidian Color Math

> Automatically apply semantic colors to LaTeX and MathJax equations in Obsidian Markdown.

Obsidian Color Math is an **official Obsidian Plugin** (and companion Python CLI) that adds scoped `\textcolor{...}{...}` wrappers to display-math blocks while preserving the original LaTeX structure.

![Before and after: an uncolored derivative becomes semantically colored by Obsidian Color Math](https://raw.githubusercontent.com/36ty-blip/obsidian-color-math/main/docs/assets/obsidian-color-math-preview.png)

*Rendered MathJax example using LaTeX generated by Obsidian Color Math.*

> [!TIP]
> **Pure TypeScript & Zero Dependencies:** The plugin runs 100% locally on Desktop & Mobile (iOS/Android), makes no network requests, collects no telemetry, and requires no external tools or AI services.

---

## 🚀 Quick Start (Obsidian Plugin)

### Installation

1. Copy `manifest.json`, `main.js`, and `styles.css` into your vault:
   ```
   <vault>/.obsidian/plugins/obsidian-color-math/
   ```
2. In Obsidian, go to **Settings > Community plugins**, find **Color Math**, and enable it.

### Usage in Obsidian

- **🎨 Ribbon Icon:** Click the palette icon on the left ribbon to open a quick-action popup menu with options to colorize or undo the current note, the active math block, selection, or open settings.
- **Command Palette:**
  - `Color Math: Colorize current math block` — Colorizes only the specific `$$...$$` block where your cursor is positioned.
  - `Color Math: Undo colors in current math block` — Reverts only the current math block back to clean LaTeX.
  - `Color Math: Colorize current note` — Replaces all uncolored display math in the note with formatted `\textcolor` equations.
  - `Color Math: Undo colors in current note` — Reverts all colored math blocks in the active note back to clean, plain LaTeX.
  - `Color Math: Colorize selection` — Colorizes only the selected equation or LaTeX block in your editor.
- **⚡ Real-Time Live Preview:** Toggle on in settings to see live syntax highlighting in your editor as you type!
- **⚙️ Settings Tab:** Customize the 10 semantic roles (`main`, `orange`, `dot`, `derivative`, `chain`, `upper`, `relation`, `arrow`, `set`, `spacing`) with visual color pickers.

---

## 💻 Quick Start (Python CLI)

If you prefer using the command line:

```bash
git clone https://github.com/36ty-blip/obsidian-color-math.git
cd obsidian-color-math
python -m pip install .
```

Preview a converted note:
```bash
color-math --file "path/to/note.md"
```

Write conversion back in place:
```bash
color-math --file --in-place "path/to/note.md"
```

Convert an expression directly:
```bash
color-math '$$\frac{d}{dx}f(y)^n=nf(y)^{n-1}\cdot f^{\prime}(y)y^{\prime}$$'
```

---

## ✨ Highlights

| Feature | Current behavior |
| --- | --- |
| **Derivatives** | Colors chain-rule stages, coefficients, powers, and selected nested forms semantically. |
| **Nested functions** | Distinguishes calls such as `y(x(g(3)))`, including `y`, `x`, and `g`, while treating `3` as a constant. |
| **Matrices & tensors** | Handles products, transpose/inverse groups, determinants, traces, norms, contractions, and tensor products. |
| **General LaTeX** | Colors operators, relations, arrows, scripts, sums, integrals, and limits. |
| **Markdown safety** | Leaves fenced code, inline code, TeX comments, and `\verb` payloads unchanged. |
| **Reversibility** | Removes both current `\textcolor` output and compatible legacy `\color` wrappers losslessly. |
| **Live Editor Highlighting** | Real-time CodeMirror 6 syntax highlighting extension in Obsidian. |

---

## 🌈 Before and After

**Input:**
```latex
$$
\frac{d}{dx}f(g(y))
=
f'(g(y))\cdot g'(y)y'
$$
```

**Generated output:**
```latex
$$
\frac{d}{dx}\textcolor{#7aa2f7}{f(g(y))}
\textcolor{white}{=}
\textcolor{#bb9af7}{f'(g(y))}\textcolor{white}{\cdot} \textcolor{#9ece6a}{g'(y)}\textcolor{#9ece6a}{y'}
$$
```

The inserted wrappers are ordinary MathJax-compatible LaTeX, so notes remain 100% portable across themes, platforms, and external Markdown renderers (GitHub, Pandoc, etc.).

---

## 🛠️ Development & Building

Building the plugin:

```bash
npm install
npm run build     # Builds production main.js
npm run test      # Runs Vitest test suite (44/44 passing)
npm run dev       # Watch mode with inline sourcemaps
```

---

## 📄 License

Released under the [MIT License](https://github.com/36ty-blip/obsidian-color-math/blob/main/LICENSE).


